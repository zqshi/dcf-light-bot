# DCF Client Suite

Cross-platform client workspace:
- `apps/web`: React + Vite web app
- `apps/desktop`: Tauri shell (placeholder)
- `apps/mobile`: Expo app (placeholder)
- `packages/*`: shared SDK/domain/tokens

## Quick start

```bash
cd client-suite
npm install
npm run dev:web
```
