name: CI

on:
  push:
    branches: ["main", "codex/**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Unit Tests
        run: npm test
      - name: Setup kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: v1.31.0
      - name: Setup Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.16.3
      - name: Validate K8s Manifests
        run: npm run check:k8s-manifests
      - name: Validate Helm Chart
        run: npm run check:helm-chart
      - name: Verify OpenClaw Lock
        run: npm run verify:openclaw-lock
