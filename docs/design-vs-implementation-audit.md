# 设计稿 vs 实现 — 全量差异审计报告

> 基于 `stitch_enterprise_im_portal_with_document_drawer/` 目录下 55+ 设计稿 与 `client-suite/apps/web/src/` 前端实现的逐页比对。
> 审计日期：2026-03-08

---

## 一、设计稿索引与组件映射

### A. 企业 IM 门户（enterprise_im_portal_with_document_drawer_1 ~ _19）

| 设计稿 | 功能描述 | 对应组件 | 实现状态 |
|--------|---------|---------|---------|
| _1 | **待办 + 任务详情右面板**：侧栏分类(今日/即将/已完成/清单)，主区任务卡片(优先级标签)，右侧详情(截止日期/关联频道/子任务/协作成员/进展同步) | `TodoPage` + `TaskDetailDrawer` | ⚠️ 部分实现 |
| _2 | **群聊 + 位置分享 Drawer**：地图预览、POI 详情(评分/距离)、路线/发送到聊天按钮 | `ChatPane` + `LocationDetailPanel` | ⚠️ 部分实现 |
| _3 | **任务详情全屏模态**：子任务列表(4项+状态标签)、任务属性(截止/优先级/负责人)、协作成员、右侧活动记录(进展同步输入+附件+图片) | `TaskDetailFullModal` | ⚠️ 部分实现 |
| _4 | **内容冲突合并**：左右双栏对比(张三修改 vs 我的修改)、差异高亮、合并并保存按钮 | `ConflictMergeView` | ⚠️ 有组件但需比对 |
| _5 | **外部订阅管理 Drawer**：AI行业助手对话 + 右侧管理面板(数据源/跟踪主题tags/发送设置toggle/时间选择) | `SubscriptionManagerPanel` | ⚠️ 部分实现 |
| _6 | **协同文档编辑**：左侧聊天+中间竖条导航+"进入编辑器"按钮+右侧富文本编辑器(实时光标/冲突检测弹窗) | `CollaborativeEditConflict` | ⚠️ 部分实现 |
| _7 | **HTML 预览 + AI 优化**：聊天+文件卡片+右侧浏览器预览(年度数据报告)+元素选取+AI优化建议浮窗 | `HTMLPreviewPanel` + `AIAssistantPanel` | ⚠️ 部分实现 |
| _8 | **驳回申请模态**：驳回理由textarea+常用理由标签(信息不全/无权限/流程不符)+取消/确认驳回 | `RejectApprovalModal` | ⚠️ 有组件 |
| _9 | **系统通知-权限已通过**：绿色check图标+文档信息卡片+立即编辑按钮+查看详情链接 | `SystemNotificationCards` (PermissionApprovedCard) | ✅ 已实现 |
| _10 | **待办扩展视图**：同_1但多了"预定下周二会议室"+"已完成"区+右侧"待办小贴士"AI卡片 | `TodoPage` | ⚠️ 缺 AI 小贴士卡片 |
| _11 | **系统通知-权限被驳回**：橙色warning+申请项目+驳回理由+审批人+再次申请/联系管理员按钮 | `SystemNotificationCards` (PermissionRejectedCard) | ✅ 已实现 |
| _12 | **协同文档编辑(活跃态)**：多用户实时光标(张三/李四)+编辑冲突检测弹窗+竖条文件导航 | 同_6 的不同状态 | ⚠️ 需比对光标细节 |
| _13 | **协同文档编辑(无冲突)**：干净编辑态，无弹窗，文件已保存+侧边导航 | 同_6 | ⚠️ |
| _14 | **审批请求+文档预览**：权限申请通知+审批卡片(批准/拒绝)+右侧财务报表预览(表格+状态标签) | `ApprovalWithDocPreview` | ⚠️ 部分实现 |
| _15 | **代码预览+AI助手**：聊天+文件卡片+右侧深色代码编辑器(JavaScript)+底部AI助手(解释/优化/问答) | `CodePreviewWithAI` | ⚠️ 部分实现 |
| _16 | **代码预览(实时同步)**：同_15但状态为"正在实时同步"+"更新到聊天"按钮+文档更新中状态 | 同_15 状态变体 | ⚠️ |
| _17 | **轻应用中心**：侧栏分类(合集/最近/收藏/分类)+主区"我的创作"(AI辅助badge)+最近使用+按分类网格(办公工具/人事服务) | `AppsGrid` + `AppsSidebar` | ⚠️ 需深度比对 |
| _18 | **行业简报详情 Drawer**：AI深度解析badge+市场关注趋势图+关键结论+潜在影响+相关研报+生成PDF按钮 | `BriefingDetailPanel` | ⚠️ 部分实现 |
| _19 | **AI应用生成预览**：左侧面板(提示词/API映射/生成日志)+右侧设备预览(桌面端/移动端toggle)+表单UI预览+完成发布按钮 | `AIAppGeneratorPreview` | ⚠️ 部分实现 |

### B. 额外门户页面（_1 ~ _5）

| 设计稿 | 功能描述 | 对应组件 | 实现状态 |
|--------|---------|---------|---------|
| _1 | **市场动态Feed(紧凑Dock)**：左侧精简Dock(消息/通讯录/日历)+Feed列表(来源标签/时间)+右侧智能分析Drawer(战略分析/市场趋势图/相关情报) | `FeedEnhancedPage` | ⚠️ 部分实现 |
| _2 | **市场动态Feed(标准Dock)**：同_1但Dock有完整图标+底部话题标签cloud | `FeedPage` 变体 | ⚠️ |
| _3 | **订阅设置模态**：数据源主题(toggle)+投递频率(radio)+时间设置+通知渠道 | `SubscriptionManagerPanel` 内模态 | ⚠️ 部分实现 |
| _4 | **知识库-个人主页**：统计卡片(笔记数/收藏/草稿箱)+常频分组+最近编辑表格(文件名/分类标签/时间/操作) | `KnowledgeHomePage` | ⚠️ 部分实现 |
| _5 | **知识库-组织资产(企业版)**：新建文档按钮+组织目录+标签筛选+最近浏览卡片网格(带头像)+全部文件表格(所有者/权限badge) | `KnowledgeAdminPage` / `FileManager` | ⚠️ 部分实现 |

### C. 知识管理系统（knowledge_management_1 ~ _20）

> 20 个设计稿覆盖完整的企业知识管理生命周期。

| 设计稿 | 功能推断 | 对应组件 | 状态 |
|--------|---------|---------|------|
| km_1 ~ km_5 | 文件管理器(列表/网格切换)、文件夹导航、权限管理 | `FileManager` / `FileListView` | ⚠️ |
| km_6 ~ km_10 | 文档编辑器(富文本)、版本历史、评论批注 | `DocumentEditorWithSettings` / `VersionHistory` | ⚠️ |
| km_11 ~ km_15 | 表格编辑器、AI公式、安全面板、部门权限 | `SpreadsheetEditor` / `DocumentSecurityPanel` | ⚠️ |
| km_16 ~ km_20 | 审计日志、存储管理、部门资产概览、编辑权限申请 | `AuditLogPage` / `StorageManagementPage` | ⚠️ |

### D. 特殊页面

| 设计稿 | 功能描述 | 对应组件 | 状态 |
|--------|---------|---------|------|
| ai | AI对话助手页面 | `ChatPane` (bot类型) | ⚠️ |
| ai_ai_html_edit_diff | AI HTML编辑差异对比 | `AIHTMLEditDiff` | ⚠️ |
| document_version_history | 文档版本历史 | `VersionHistoryPanel` | ⚠️ |
| real_time_comments | 实时评论/批注 | `RealTimeComments` | ⚠️ |
| spreadsheet_collaborative_edit | 表格协同编辑 | `SpreadsheetEditor` | ⚠️ |
| spreadsheet_preview | 表格预览 | `SpreadsheetView` | ⚠️ |

### E. OpenClaw 暗色面板（openclaw_*）

| 设计稿 | 功能描述 | 对应组件 | 状态 |
|--------|---------|---------|------|
| openclaw_ai_1 | **完整主面板**：顶栏(状态/IM通道指示器/令牌计数/时钟/用户)+Agent侧栏+CoT对话+右侧任务面板(通知3/进度条/柱状图/AI洞察) | `OpenClawPage` 全集成 | ⚠️ 有差异 |
| openclaw_ai_2 | **精简主面板**：同上但右侧面板无通知区域，仅进度+图表+洞察 | `OpenClawPage` 基础态 | ⚠️ 有差异 |
| openclaw_ai_3 | **IM快速回复弹窗**：深色模态，原始消息+回复输入+快捷按钮(收到明白了/正在检查/让我确认)+发送 | `IMReplyModal` | ⚠️ 有差异 |
| openclaw_1 | **主面板+任务详情抽屉**：三栏(Agent侧栏/对话区+任务面板/任务详情)+子任务/执行日志/生成资源/操作按钮 | `OpenClawPage` + `TaskDetailDrawer` | ⚠️ 有差异 |
| openclaw_2 | **任务全屏详情**：左侧CoT对话+右侧(84%环形进度/子任务状态badge/执行日志/停止任务+生成报告) | `TaskDetailView` | ⚠️ 有差异 |
| openclaw_v2 | **OpenClaw v2 欢迎页**：全新设计(头像Agent列表/欢迎语/快捷指令网格/控制面板看板/系统算力状态/专业版标识) | **完全缺失** | ❌ 未实现 |

---

## 二、关键差异清单（按严重程度排序）

### 🔴 P0 — 完全缺失的页面/功能

| # | 缺失项 | 设计稿 | 说明 |
|---|--------|--------|------|
| 1 | **OpenClaw v2 欢迎页** | `openclaw_v2` | 全新设计：Agent列表带头像+状态badge、欢迎语、4个快捷指令卡片(总结邮件/检查服务器/安排周会/竞品调研)、控制面板看板(活跃Agent数/系统延迟)、最近任务列表、快捷资源链接、底部系统算力条+专业版标识。当前完全没有此页面。 |
| 2 | **知识管理系统独立布局** | `_4`, `_5` | 设计稿显示知识库有独立的布局结构：左侧是知识库专属Dock(首页/知识/消息/团队/发现/统计/设置)，不是主IM的Dock。当前实现使用主IM Dock的knowledge tab。 |

### 🟠 P1 — 布局/结构性差异

| # | 差异项 | 设计稿 | 当前实现 | 差距 |
|---|--------|--------|---------|------|
| 1 | **OpenClaw 顶栏 IM通道指示器** | 3个彩色方块(绿/红/蓝)+文字标签(飞书/Slack等) | 3个圆点无标签 | 指示器形状和标签缺失 |
| 2 | **OpenClaw 顶栏令牌计数** | `令牌计数器：1,240,892` 带图标+绿色pill | `💰1,240,892` 用emoji | 应该用Icon+pill样式 |
| 3 | **OpenClaw Agent侧栏** | Agent卡片有完整的40x40圆形头像+彩色背景+状态点在头像右下 | 用Icon替代头像，状态点在文字旁 | 头像系统完全不同 |
| 4 | **OpenClaw CoT步骤** | 步骤间有竖线连接(timeline style) | 步骤间只有间距 | 缺少timeline连接线 |
| 5 | **OpenClaw 任务面板通知卡片** | 每个通知有来源icon(Lark方块/钉钉方块)+来源名+时间 | 只有source+sender文字 | 缺少来源icon |
| 6 | **OpenClaw 柱状图** | 设计稿是真实的分时柱状图(08:00~12:00) | 实现是简化的4条bar | 样式和数据差距大 |
| 7 | **OpenClaw 任务详情抽屉** | 设计稿同时显示在中间任务面板旁边 | 实现是覆盖式overlay | 布局方式不同 |
| 8 | **OpenClaw 全屏任务详情** | 左侧Agent不同(渗透测试专家/流量分析员/应急响应助手)+不同Dock icons(终端/应用/漏洞/层级/图表/设置) | 用通用Agent列表+通用Dock | Agent角色和Dock未适配 |
| 9 | **待办任务详情右面板** | 设计稿有子任务checklist(含checkbox)+关联文档链接+协作成员头像组+进展同步富文本编辑器(B/I/链接/@)+附件按钮+发布按钮 | 实现缺少进展同步编辑器 | 缺核心交互区 |
| 10 | **轻应用中心** | "我的创作"区有`Edit with AI` badge+分类侧栏有图标+最近使用横向卡片滚动+分类网格6列 | 需要全面比对 | 可能有多处细节差异 |
| 11 | **市场动态Feed** | 文章卡片有来源彩色标签(彭博社/科技节点/华尔街日报等)+话题tag chip+时间 | 需要全面比对 | 来源标签系统 |
| 12 | **订阅管理Drawer** | 数据源卡片+跟踪主题tag+发送设置toggle+时间picker+底部状态栏 | 需要全面比对 | 可能有差异 |

### 🟡 P2 — 样式/细节差异

| # | 差异项 | 设计稿 | 当前实现 |
|---|--------|--------|---------|
| 1 | **OpenClaw 发送按钮** | 圆形绿色(#00D4B8) | 实现一致 ✅ |
| 2 | **OpenClaw 玻璃面板背景** | `rgba(10,15,30,0.7)` + blur(12px) | 实现 `bg-white/[0.02]` 偏暗 |
| 3 | **OpenClaw 进度条辉光** | 设计稿有明显的 `box-shadow: 0 0 10px` 辉光 | 实现有辉光 ✅ |
| 4 | **OpenClaw 通知badge** | 右下角红色圆圈带数字 | 实现一致 ✅ |
| 5 | **IMReplyModal 快捷回复** | "正在检查，请稍等" | 实现是"正在检查" | 文案不一致 |
| 6 | **全局 Dock 图标** | 设计稿部分页面Dock只有4-6个图标 | 实现有完整8+2 | 不同页面Dock图标集不同 |
| 7 | **系统通知卡片边框** | 设计稿有更大的圆角+微妙阴影 | 实现使用border | 视觉差异 |
| 8 | **聊天气泡** | 设计稿发送气泡为深紫色(IM模式) | 实现为primary蓝色 | 颜色可能需对齐 |
| 9 | **文档编辑器工具栏** | B I 列表 链接 图片 AI | 需比对每个按钮 | |
| 10 | **代码预览暗色主题** | 深色编辑器+语法高亮 | 需比对颜色方案 | |

---

## 三、OpenClaw 专项差异（openclaw_ai_1 vs 实现）

### 3.1 顶栏 (`OpenClawHeader`)

| 元素 | 设计稿(openclaw_ai_1) | 当前实现 | 修复 |
|------|----------------------|---------|------|
| Logo | `🤖 OpenClaw` 绿色图标 | `smart_toy` 图标 | ✅ 一致 |
| 代理状态 | `● 代理状态: 运行中` | `代理状态: 运行中` | ✅ 一致 |
| IM通道 | `IM 通道:` + 3个彩色方块(■绿 ■红 💬蓝) + tooltip | `IM 通道:` + 3个圆点 | 🔧 改为方块+增加tooltip |
| 令牌计数 | `🪙 令牌计数器：1,240,892` 绿色pill背景 | `💰1,240,892` amber颜色 | 🔧 改为绿色+pill样式 |
| 时钟 | `12:45:08 PM` | 一致 | ✅ |
| 用户 | `ADMINISTRATOR` 小字 + `Alex Chen` + 圆形头像 | 一致 | ✅ |

### 3.2 Agent侧栏 (`OpenClawSidebar`)

| 元素 | 设计稿 | 当前实现 | 修复 |
|------|--------|---------|------|
| Agent头像 | 40x40 圆形 + 彩色背景(蓝/青/紫/橙) + 图标 | 36x36 圆角方形 + 浅色背景 + 图标 | 🔧 改为圆形+彩色 |
| 状态指示 | 绿色圆点在头像右下角(8x8) | 文字旁1.5x1.5圆点 | 🔧 移到头像上 |
| 选中态 | 整卡蓝绿色边框+深色背景 | 浅绿背景+绿色边框 | ⚠️ 接近 |
| 底部 | `❓帮助` + `⚙️系统配置` 分隔线上方 | 实现一致 | ✅ |
| Dock图标 | 设计稿左侧有独立Dock(消息/应用/通讯录/文件夹) | 共用主Dock | 🔧 OpenClaw Dock items需对齐 |

### 3.3 CoT对话区 (`OpenClawPage`)

| 元素 | 设计稿 | 当前实现 | 修复 |
|------|--------|---------|------|
| 用户气泡 | 右对齐 + 背景色偏深 + 用户头像按钮(右上) | 右对齐 ✅ | 🔧 缺用户头像按钮 |
| CoT区域 | 有图标(🧠) + 步骤间有timeline竖线 + 绿色渐变底部进度条 | 图标+步骤但无竖线 | 🔧 添加timeline线 |
| Agent回复 | 代码高亮 `auth-service.js` 用单独颜色 | 纯文本 | 🔧 添加内联代码高亮 |
| 输入区 | `📎 🎤` 左侧 + 文本 + 圆形绿色发送按钮 | 一致 | ✅ |

### 3.4 任务面板 (`TaskMonitorPanel`)

| 元素 | 设计稿 | 当前实现 | 修复 |
|------|--------|---------|------|
| 全局通知badge | 红色圆圈带数字`3` 在标题旁 | 青色pill背景 | 🔧 改为红色圆形badge |
| 通知卡片 | 来源彩色方块(■ Lark) + 名字 + 时间 + 回复按钮 | 纯文本来源 | 🔧 添加来源方块icon |
| 进度条 | 标签左+百分比右+进度条+详情+子信息 | 一致 | ✅ |
| 柱状图 | 时间轴横坐标(08:00~12:00)+多条分时柱 | 4条简单bar | 🔧 改为时间轴柱状图 |
| AI洞察 | 有开关toggle在右上 | 无toggle | 🔧 添加toggle |

### 3.5 OpenClaw v2 欢迎页 (`openclaw_v2`) — 完全缺失

设计稿展示的全新页面：
- **顶栏**：`OpenClaw AI` + `● 代理在线` badge + `飞书 钉钉` IM图标 + TOKEN消耗(1,284,042/5.0M) + 全局搜索框 + 通知铃 + 用户头像
- **Agent列表**：真实头像+姓名+描述+状态badge(空闲/任务中/离线)
- **欢迎区**：大星形图标 + "下午好, 管理员" + 描述文字 + 4个快捷指令卡片(2x2网格)
- **控制面板看板**：系统健康(活跃Agent:12, 系统延迟:42ms) + 最近任务列表(3条) + 快捷资源链接
- **底部**：系统算力状态(324 TFLOPS, 负载:良好) + 专业版已激活(有效期2025-12-31)
- **输入栏**：麦克风+附件+闪电+文本+发送

---

## 四、IM 模式主要差异

### 4.1 Dock 导航栏

| 差异 | 设计稿 | 实现 |
|------|--------|------|
| 图标数量 | 不同页面的Dock图标数不同(4~10) | 固定8+2 |
| Logo | 紫色渐变圆角Logo | 蓝色渐变 |
| 底部图标 | 部分设计稿有"邮件"(mail)图标 | 无邮件 |

### 4.2 聊天 / 消息

| 差异 | 设计稿参考 | 实现 |
|------|----------|------|
| 发送气泡颜色 | 深紫色(_5,_6,_12) / 蓝色(_9,_14) | 蓝色(primary) |
| 文件卡片 | 紫色图标+状态badge(已编辑/已同步)+打开编辑器按钮+下载按钮 | 需比对 |
| RoomList tab | "全部/群组/订阅号●" 3个tab | 需确认是否存在 |
| 编辑按钮(✏️) | 右上角新建对话图标 | 需确认 |
| 头部按钮组 | 电话📞+历史🕐+详情ℹ️ | 需确认 |

### 4.3 审批/通知卡片

| 差异 | 设计稿 | 实现 |
|------|--------|------|
| 审批卡片 | _14显示：申请编辑权限+申请理由+查看文档链接+批准/拒绝按钮 | `ApprovalWithDocPreview`有实现 |
| 驳回申请模态 | _8显示：居中白色模态+理由textarea+常用理由tags | `RejectApprovalModal`有实现 |
| 权限通过通知 | _9显示：绿色check+文档卡片+立即编辑 | `PermissionApprovedCard`已实现 ✅ |
| 权限驳回通知 | _11显示：橙色warning+驳回理由+再次申请/联系管理员 | `PermissionRejectedCard`已实现 ✅ |

### 4.4 待办 / 任务

| 差异 | 设计稿(_1,_3,_10) | 实现 |
|------|-------------------|------|
| 侧栏结构 | 今日待办(5)/即将到来(12)/已完成 + 清单分组(工作项目/个人生活) | 需比对 |
| 任务卡片 | 优先级红色badge+时间+关联频道/文档链接 | 需比对 |
| 任务详情面板 | 截止日期+关联频道链接+关联文档+子任务checklist+协作成员头像+进展同步编辑器 | 缺进展同步编辑器 |
| AI小贴士卡片 | _10右下角"✨待办小贴士"渐变卡片 | 缺失 |
| 全屏任务详情 | _3显示模态弹窗形式，左右分栏 | 需比对TaskDetailFullModal |

### 4.5 代码预览

| 差异 | 设计稿(_15,_16) | 实现 |
|------|----------------|------|
| 代码编辑器 | 深色背景+行号+语法高亮+语言下拉选择+复制按钮 | 需比对 |
| AI助手 | "解释这段代码"+"优化建议"+自由问答+模型选择(RK图标) | 需比对 |
| 实时同步状态 | _16显示"正在实时同步"+"更新到聊天"按钮 | 需确认 |

### 4.6 HTML预览

| 差异 | 设计稿(_7) | 实现 |
|------|----------|------|
| 浏览器chrome | 红黄绿圆点+地址栏+前进后退 | 需比对 |
| 元素选取模式 | "元素选取"绿色按钮 | 需确认 |
| AI优化建议浮窗 | 底部浮窗+输入框+快捷标签(极简/玻璃拟态/加宽) | 需比对 |

---

## 五、知识管理系统差异概述

> 20个设计稿，是整个系统中最复杂的模块。需要逐个读取后详细比对。

预期关键差异点：
1. **独立Dock布局** — 设计稿(_4,_5)显示知识库有自己的Dock导航体系
2. **文件管理器** — 网格/列表视图切换、拖拽、权限badge
3. **富文本编辑器** — 工具栏按钮集、实时协作光标
4. **表格编辑器** — 单元格编辑、AI公式、协同编辑
5. **版本历史** — 时间线对比
6. **安全面板** — 部门权限矩阵
7. **审计日志** — 操作记录表格
8. **存储管理** — 配额饼图/柱状图

---

## 六、优先修复建议

### Phase 1 — OpenClaw 设计还原（最高优先）

1. **新建 `OpenClawWelcomePage`** — 实现 `openclaw_v2` 欢迎页
2. **修复 `OpenClawHeader`** — IM通道方块、令牌计数绿色pill
3. **修复 `OpenClawSidebar`** — 圆形彩色头像、状态点位置
4. **修复 `OpenClawPage` CoT区** — timeline竖线、代码高亮
5. **修复 `TaskMonitorPanel`** — 红色通知badge、来源图标、时间轴柱状图
6. **修复 `IMReplyModal`** — 快捷回复文案

### Phase 2 — IM 核心功能还原

7. **完善 `TodoPage` 任务详情** — 进展同步编辑器、AI小贴士卡片
8. **完善 `ChatPane` 文件卡片** — 状态badge、编辑器打开按钮
9. **对齐 `AppsGrid`** — "我的创作"AI badge、分类网格
10. **对齐 `FeedPage`** — 来源彩色标签、分析Drawer

### Phase 3 — 知识管理系统

11-30. 逐个对齐 20 个知识管理页面
